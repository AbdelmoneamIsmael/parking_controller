enum SnackBarType {
  success,
  error,
  warning,
  info,
}

enum InvoicePaidStatus {
  unpaid,
  partial,
  paid,
}

enum SortType {
  asc,
  desc,
}

enum DiscountType {
  percentage,
  amount,
}

enum OrderStatus {
  pending,
  processing,
  shipped,
  delivered,
  canceled,
}

enum SocialLogin {
  facebook,
  google,
}
